
@{
    ViewBag.Title = "Staffs";
}

<div class="container">
    <div class="row">
        <div class="col l12 m12 s12">
            <h5>Assigned Staffs</h5>
            <div class="card white darken-1">
                <div class="card-content">
                    <table id="type_complaint" class="responsive-table">
                        <tr>
                            <th>Staff Name</th>
                            <th>*</th>

                        </tr>
                        @foreach (staff_assigend staff in Model)
                        {
                            <tr>
                                <td>@staff.Name</td>
                                <td>
                                    <a href="@Url.Action("DeleteStaff","MasterData",new { ID = staff.ID })" class="btn red"><i class="material-icons">delete</i></a>
                                    <a class="btn red"><i class="material-icons">edit</i></a>
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>

<div class="fixed-action-btn">
    <a class="btn-floating btn-large red add modal-trigger" data-target="add_staff">
        <i class="large material-icons">add_circle</i>
    </a>
</div>


